# https://github.com/athityakumar/colorls

# Ensure that ruby is installed
which ruby > /dev/null || return

# Note for iTerm2 users - Please enable the Nerd Font at iTerm2 > Preferences > Profiles > Text > Non-ASCII font > Knack Regular Nerd Font Complete.

# Ensure that color ls is installed
#which colorls > /dev/null || sudo gem install colorls

#source "$(dirname $(gem which colorls))/tab_complete.sh"

#alias lc='colorls --dark --gs'
#alias tree='colorls --tree --gs'